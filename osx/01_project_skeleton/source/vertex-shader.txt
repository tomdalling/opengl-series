#version 150

uniform mat4 combinedTransformationMatrix;

in vec4 vert;
in vec4 vertColor;

out vec4 fragColor;

void main() {
    // Pass the color straight through to the fragment shader
    fragColor = vertColor;
    
    // One matrix tranformation
    gl_Position = combinedTransformationMatrix * vert;
}